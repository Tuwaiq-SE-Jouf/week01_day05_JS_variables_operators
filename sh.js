function calcGeometry(radius){
    var circumference = math.pi*
    2*radius;
    console.log("the circumference is "+circumference   );
    var area=Math.pl*radius*radius;
    console.log("the area is " + area);

}